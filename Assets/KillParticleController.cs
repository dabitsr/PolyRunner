using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KillParticleController : MonoBehaviour
{
    ParticleSystem p;

    // Start is called before the first frame update
    void Start()
    {
        p.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
